#include <iostream>
#include <cmath>
#include <string>
#include <fstream>

using namespace std;

int main() {

	int maxPages, printerSpeed, numJobs, numPrinters;
	cout << "max number of pages: ";
	cin >> maxPages;
	cout << "printer speed: ";
	cin >> printerSpeed;
	cout << "number of jobs: ";
	cin >> numJobs;
	cout << "number of printers: "
	cin >> numPrinters;

	cout <"The swag never stops\n";

	return 0;
}
